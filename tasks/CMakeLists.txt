# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sonar_sog_slamTaskLib)
ADD_LIBRARY(${SONAR_SOG_SLAM_TASKLIB_NAME} SHARED 
    ${SONAR_SOG_SLAM_TASKLIB_SOURCES})
add_dependencies(${SONAR_SOG_SLAM_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SONAR_SOG_SLAM_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SONAR_SOG_SLAM_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SONAR_SOG_SLAM_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SONAR_SOG_SLAM_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SONAR_SOG_SLAM_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SONAR_SOG_SLAM_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SONAR_SOG_SLAM_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SONAR_SOG_SLAM_TASKLIB_HEADERS}
    DESTINATION include/orocos/sonar_sog_slam)

